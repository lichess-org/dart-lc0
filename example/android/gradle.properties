org.gradle.jvmargs=-Xmx4G
android.useAndroidX=true
android.enableJetifier=true
# AGP 9's built-in Kotlin compiles this app's one Kotlin source, so the Kotlin
# Gradle Plugin is not applied. Set explicitly: the Flutter tool appends
# `android.builtInKotlin=false` to any gradle.properties that does not mention
# the flag at all.
android.builtInKotlin=true
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
